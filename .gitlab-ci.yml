stages:
  - deploy

deploy_job:
  stage: deploy
  only:
    - main
  script:
    - cd /builds/lukasmetzler/echtzeitvisualisierung-von-gebaeudeindustriedaten
    - git pull
    - docker-compose down
    - docker-compose up -d
  tags:
    - hella
