stages:
  - deploy

deploy_job:
  stage: deploy
  only:
    - main
  script:
    - cd /builds/lukasmetzler/echtzeitvisualisierung-von-gebaeudeindustriedaten
    - git status
    - git fetch
    - git checkout main
    - git pull origin main
    - docker-compose down
    - docker-compose up -d
  tags:
    - hella
