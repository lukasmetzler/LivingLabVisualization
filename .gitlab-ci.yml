image: docker:stable

stages:
  - build
  - deploy

variables:
  IMAGE_NAME: latest

before_script:
  - docker --version

build:
  stage: build
  script:
    - docker build -t $CI_REGISTRY_IMAGE:$IMAGE_TAG .
