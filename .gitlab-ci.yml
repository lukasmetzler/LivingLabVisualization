stages:
  - deploy

deploy_job:
  stage: deploy
  only:
    - main
  script:
    - pwd
    - ls -la
    - cd /builds/lukasmetzler/echtzeitvisualisierung-von-gebaeudeindustriedaten
    - pwd
    - ls -la
    - git fetch
    - git checkout main
    - git pull origin main
    - docker-compose down
    - docker-compose up -d
  tags:
    - hella
